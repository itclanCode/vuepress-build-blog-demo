// 覆盖主题默认样式,将会被自动应用的全局样式文件，会生成在最终的 CSS 文件结尾，具有比默认样式更高的优先级
.page {
  padding-left: 0;
}

.theme-default-content code {
  background-color: #fff5f5;
  color: #FF0000;
}

#valine-vuepress-comment {
  width: 50%;
  margin: auto;
}

.txt-right{
  display:none;
}

